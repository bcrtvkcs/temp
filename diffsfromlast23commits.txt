Tüm commit'ler: https://gitlab.com/simonpunk/susfs4ksu/-/commits/gki-android16-6.12

# b917aaca'nın tüm etkilerini özetleyeyim:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/b917aaca61f99f1a30a97d7b4f31222090613449

kernel_patches/fs/susfs.c:

susfs_is_boot_completed_triggered → susfs_is_sdcard_android_data_decrypted rename
kthread bazlı monitor kaldırılıp fsnotify'a geçiş (ama bu sonraki commit'lerde daha çok değişti)
susfs_start_sdcard_monitor_fn() eklendi

kernel_patches/include/linux/susfs.h:

susfs_start_sdcard_monitor_fn() declaration eklendi

kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch:

supercalls.c'de susfs_is_boot_completed_triggered declaration → susfs_is_sdcard_android_data_decrypted
susfs_is_boot_completed_triggered = true → kaldırılıp yerine susfs_start_sdcard_monitor_fn() çağrısı

Bizim kernel'daki durum:

fs/susfs.c ✅ — susfs_is_sdcard_android_data_decrypted ve susfs_start_sdcard_monitor_fn mevcut
include/linux/susfs.h ✅ — susfs_start_sdcard_monitor_fn declaration mevcut
drivers/kernelsu/supercalls.c ❌ — hâlâ susfs_is_boot_completed_triggered var, susfs_start_sdcard_monitor_fn() çağrısı yok

# 1075550d commit'inin tüm etkileri:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/1075550d96981458ce989fa5875e5ace12179a13

Etkilenen dosya: kernel_patches/fs/susfs.c (sadece bu)
Değişiklikler:

#include <linux/fsnotify_backend.h> eklendi
#define SDCARD_ANDROID_DATA_PATH "/sdcard/Android/data" → "/sdcard/Android"
#define SDCARD_MONITOR_INTERVAL_MS 5000 kaldırıldı
#define SDCARD_MONITOR_MAX_ATTEMPTS 60 kaldırıldı
static struct task_struct *susfs_sdcard_monitor_thread; satırı bool susfs_is_sdcard_android_data_decrypted satırının üstünden altına taşındı
kthread tabanlı monitor tamamen kaldırılıp fsnotify tabanlı implementasyon eklendi: watch_dir struct, g, g_watch (/sdcard, FS_CREATE | FS_MOVE | FS_EVENT_ON_CHILD), watch_one_dir(), unwatch_one_dir(), susfs_handle_sdcard_inode_event(), fsnotify_ops, add_mark_on_inode()
susfs_sdcard_monitor_fn() içi tamamen değişti — kthread loop yerine fsnotify_alloc_group + watch_one_dir kullanımı, #if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 0, 0) koşulu
susfs_start_sdcard_monitor_fn() içinde log mesajı küçük harfe düzeltildi

Bizim kernel'daki durum: ✅ Mevcut (daha ileri bir versiyona sahip)

# caad8917 commit'inin tüm etkileri:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/caad8917a5c5241d08f606941dfb892b5ec6d789

Etkilenen dosya: kernel_patches/fs/susfs.c (sadece bu)
Değişiklikler:

add_mark_on_inode forward declaration eklendi (g_watch tanımından sonra, watch_one_dir'dan önce)
kern_path(wd->path, LOOKUP_FOLLOW, &wd->kpath) → kern_path(wd->path, 0, &wd->kpath)

Bizim kernel'daki durum: ⚠️ Kısmen — bizde LOOKUP_FOLLOW var (kern_path(wd->path, LOOKUP_FOLLOW, ...)) ama 6cadfcf bu değişikliği geri aldı (0 → LOOKUP_FOLLOW). Forward declaration bizde mevcut. Net sonuç: LOOKUP_FOLLOW doğru, bu commit'in etkisi 6cadfcf tarafından geri alındı. ✅

# 6cadfcf commit'inin tüm etkileri:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/6cadfcf60977d937be45c7b1d6170ea82d7eb9c7

Etkilenen dosya: kernel_patches/fs/susfs.c (sadece bu)
Değişiklikler:

#define SDCARD_ANDROID_DATA_PATH "/sdcard/Android" → "/data/media/0/Android"
static struct task_struct *susfs_sdcard_monitor_thread; satırı kaldırıldı
g_watch.path → "/data/media/0", mask → (FS_EVENT_ON_CHILD | FS_ISDIR | FS_OPEN_PERM)
kern_path(wd->path, 0, ...) → kern_path(wd->path, LOOKUP_FOLLOW, ...)
unwatch_one_dir içinde iput öncesi path_put sırası değişti
susfs_handle_sdcard_inode_event içinde:

if (!file_name) return 0; ve if (mask & FS_ISDIR) return 0; → if (target_path_is_found || !file_name) return 0;
cleanup değişti: unwatch_one_dir + fsnotify_put_group kaldırılıp yerine fsnotify_destroy_group(g) + iput(g_watch.inode) + path_put(&g_watch.kpath) geldi
log mesajları güncellendi


susfs_sdcard_monitor_fn içinde susfs_sdcard_monitor_thread = NULL; satırı kaldırıldı
susfs_start_sdcard_monitor_fn içinde susfs_sdcard_monitor_thread = kthread_run(...) → kthread_run(...) (dönüş değeri atanmıyor)

Bizim kernel'daki durum: ✅ Mevcut (daha ileri bir versiyon)

# cde2cfd commit'inin tüm etkileri:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/cde2cfd3da0f30af0e47c2a13a1656a7eb28af3e

Etkilenen dosya: kernel_patches/50_add_susfs_in_gki-android16-6.12.patch (sadece bu)
Değişiklikler (6.12 namespace.c için):

susfs_ksu_mnt_group_ida kaldırıldı
mnt_alloc_group_id: susfs_ksu_mnt_group_ida yerine mnt_group_ida kullanımı, koşul !susfs_is_sdcard_android_data_decrypted && mnt->mnt_id >= DEFAULT_KSU_MNT_ID → susfs_is_current_ksu_domain() oldu
mnt_release_group_id hook'u kaldırıldı

Bizim kernel'daki durum: Bu commit 6.12 patch dosyasını etkiliyor. Bizim 5.4 namespace.c implementasyonumuzda susfs_mnt_group_ida var ve mnt_release_group_id hook'u var — bunlar 26e8a81'den geliyor ve 26e8a81 cde2cfd'den sonra geliyor, yani 26e8a81 tekrar ayrı IDA'ya döndü. Bizim implementasyonumuz 26e8a81 bazlı — ✅ doğru.
5.4 için yapılacak bir şey yok.

# 2242ee2 commit'inin tüm etkileri:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/2242ee2409d9c75fadf98d55dc957aef03d489d9

kernel_patches/include/linux/susfs_def.h:

AS_FLAGS_ANDROID_DATA_ROOT_DIR 37, AS_FLAGS_SDCARD_ROOT_DIR 38 kaldırıldı
BIT_SUS_PATH, BIT_SUS_MOUNT, BIT_SUS_KSTAT, BIT_OPEN_REDIRECT, BIT_ANDROID_DATA_ROOT_DIR, BIT_ANDROID_SDCARD_ROOT_DIR, BIT_SUS_MAPS kaldırıldı
ND_STATE_LOOKUP_LAST 32, ND_STATE_OPEN_LAST 64, ND_STATE_LAST_SDCARD_SUS_PATH 128 kaldırıldı
susfs_is_current_proc_umounted_app() eklendi (sonradan eklendi — diff'te görünmüyor ama +5 var)
kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch:

KSU_SUSFS_SUS_PATH default y → n
KSU_SUSFS_SUS_KSTAT default y → n
KSU_SUSFS_OPEN_REDIRECT default y → n
kernel_patches/fs/susfs.c: Büyük refactor — preview limit aşıldı, tam içeriği göremedik.

kernel_patches/50_add_susfs_in_gki-android16-6.12.patch: 6.12'ye özgü — 5.4'ü etkilemiyor.

Bizim kernel'daki durum:

susfs_def.h ✅ — bu define'lar bizde zaten yok, susfs_is_current_proc_umounted_app var
KernelSU Kconfig default'ları — kontrol etmemiz lazım
susfs.c — tam diff'i göremedik
bash
grep -n "KSU_SUSFS_SUS_PATH\|KSU_SUSFS_SUS_KSTAT\|KSU_SUSFS_OPEN_REDIRECT" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/Kconfig | head -10

blackshark@blackshark:~/susfs4ksu$ grep -n "KSU_SUSFS_SUS_PATH\|KSU_SUSFS_SUS_KSTAT\|KSU_SUSFS_OPEN_REDIRECT" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/Kconfig | head -10
54:config KSU_SUSFS_SUS_PATH
74:config KSU_SUSFS_SUS_KSTAT
121:config KSU_SUSFS_OPEN_REDIRECT

# f0dccf2 commit'inin tüm etkileri:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/f0dccf22721495e36a986f5b8cd786f9061fefc8

Etkilenen dosya: kernel_patches/50_add_susfs_in_gki-android16-6.12.patch (sadece bu)
Değişiklikler: fs/proc/task_mmu.c'de pagemap_read() içindeki CONFIG_KSU_SUSFS_SUS_MAP hook'u mmap_read_unlock() çağrıları arasına taşındı — race fix.
Bizim kernel'daki durum: Bizim fs/proc/task_mmu.c'yi kontrol etmemiz lazım:
bashgrep -n "KSU_SUSFS_SUS_MAP\|mmap_read_unlock\|pagemap_read" ~/aesir_kernel_oneplus_sm8350/fs/proc/task_mmu.c | head -15

blackshark@blackshark:~/susfs4ksu$ grep -n "KSU_SUSFS_SUS_MAP\|mmap_read_unlock\|pagemap_read" ~/aesir_kernel_oneplus_sm8350/fs/proc/task_mmu.c | head -15

30:#if defined(CONFIG_KSU_SUSFS_SUS_KSTAT) || defined(CONFIG_KSU_SUSFS_SUS_MAP)

376:#ifdef CONFIG_KSU_SUSFS_SUS_MAP

949:#ifdef CONFIG_KSU_SUSFS_SUS_MAP

974:#ifdef CONFIG_KSU_SUSFS_SUS_MAP

988:#ifdef CONFIG_KSU_SUSFS_SUS_MAP

1707:static ssize_t pagemap_read(struct file *file, char __user *buf,

1818:	.read		= pagemap_read,

# 624a087 commit'inin tüm etkileri:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/624a0877aa97191f79c124c254c210a0cda8dc83

Etkilenen dosya: kernel_patches/50_add_susfs_in_gki-android16-6.12.patch (sadece bu)
Değişiklikler (6.12'ye özgü):

fs/namei.c ve fs/readdir.c'de extern declaration susfs_is_inode_sus_path(struct inode *inode) → susfs_is_inode_sus_path(struct mnt_idmap* idmap, struct inode *inode) — 6.12'ye özgü, bizim 5.4'te zaten struct inode *inode versiyonu doğru
fs/namespace.c'de susfs_reorder_mnt_id() içinde for (; r = first; ...) → for (r = first; r; ...) syntax fix ve NULL check eklendi — ama bu susfs_ksu_mounts ve listmnt_next kullanan 6.12'ye özgü implementasyon, 5.4'te yok

Bizim kernel'daki durum: 5.4'ü etkilemiyor. ✅

# db2ba6b6 commit'inin tüm etkileri:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/db2ba6b625bf19a92c733871de056b3f02669526

Etkilenen dosya: kernel_patches/50_add_susfs_in_gki-android16-6.12.patch (sadece bu)

Değişiklikler: init/main.c'de start_kernel() içine recovery mode detection eklendi — saved_command_line'da "=recovery" varsa susfs_is_sdcard_android_data_decrypted = true set ediliyor.

Bizim kernel'daki durum: Bizim init/main.c'yi kontrol etmemiz lazım:

bash
grep -n "susfs\|recovery" ~/aesir_kernel_oneplus_sm8350/init/main.c | head -10

Boş döndü

Bizde init/main.c'de recovery mode detection yok. Eksik — not edelim.
Ama dikkat: bu commit db2ba6b6 bir "Test" commit'i ve bc85d1cc ile revert edildi. Uygulamak yerine revert commit'ini de görelim, sonra karar verelim.

# e77b280d — strstr argüman sırası düzeltildi. db2ba6b6 + e77b280d birlikte şu kodu ekliyor init/main.c'ye:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/e77b280df722296f4531f4b96f8cbd6ece9f55be

c#ifdef CONFIG_KSU_SUSFS
	if (strstr(saved_command_line, "=recovery")) {
		pr_info("susfs: kernel is likely run in recovery mode, set susfs_is_sdcard_android_data_decrypted to true to stop assigning sus mnt_id/mnt_group_id\n");
		susfs_is_sdcard_android_data_decrypted = true;
	}
#endif
Sonraki iki commit a61b813b (revert e77b280d) ve bc85d1cc (revert db2ba6b6) — ikisi de revert. Yani net sonuç: bu değişiklik geri alındı, init/main.c'ye hiçbir şey eklememiz gerekmiyor. ✅

# a61b813b Revert "Test: kernel: Fix "recovery" string not detected":

https://gitlab.com/simonpunk/susfs4ksu/-/commit/a61b813b095e2e1cf78d69af000519cff8e49b7d

a61b813b — e77b280d'yi revert ediyor, beklediğimiz gibi. 

# bc85d1cc Revert "Test: kernel: Attempt to fix bootloop when booting into recovery by adding recovery mode detection in cmdline"

https://gitlab.com/simonpunk/susfs4ksu/-/commit/bc85d1cc2be45b8a5c1afb538dbeadf4215bf7a8

bc85d1cc — db2ba6b6'yı revert ediyor. init/main.c değişikliği tamamen geri alındı. Net sonuç: init/main.c'ye hiçbir şey eklememiz gerekmiyor. ✅

# 96ad091 commit'inin tüm etkileri:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/96ad09152aefbd98a55739744737cf4989e9b640

Etkilenen dosya: kernel_patches/50_add_susfs_in_gki-android16-6.12.patch (sadece bu)
Değişiklikler: susfs_reorder_mnt_id() içinde listmnt_next() → rb_next(node) ile node iteration — 6.12'ye özgü, mnt_namespace->mounts rb_tree, node_to_mount(), susfs_mnt_id_backup gibi 6.12 API'ları kullanıyor.
Bizim kernel'daki durum: 5.4'ü etkilemiyor. ✅

# 754a62f commit'inin tüm etkileri:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/754a62f5cf12e6d3d178d53a66b3cb337f8d8d63

Etkilenen dosya: kernel_patches/50_add_susfs_in_gki-android16-6.12.patch (sadece bu)
Değişiklikler — tamamı 6.12'ye özgü API kullanıyor:

mnt_id_unique, atomic64_inc_return(&mnt_id_ctr) — 5.4'te yok
susfs_mnt_id_backup, mnt_find_id_at(), mnt_find_id_at_reverse() — 5.4'te yok
do_listmount(), lookup_mnt_in_ns() — 5.4'te yok
susfs_reorder_mnt_id() içinde mnt_id_unique reorder — 5.4'te yok
fs/proc_namespace.c'de r->mnt_id >= DEFAULT_KSU_MNT_ID → r->mnt_id == DEFAULT_KSU_MNT_ID fix

fs/proc_namespace.c değişikliği 5.4'ü etkiliyor mu?
bashgrep -n "DEFAULT_KSU_MNT_ID\|susfs_hide_sus_mnts" ~/aesir_kernel_oneplus_sm8350/fs/proc_namespace.c | head -10

blackshark@blackshark:~/susfs4ksu$ grep -n "DEFAULT_KSU_MNT_ID\|susfs_hide_sus_mnts" ~/aesir_kernel_oneplus_sm8350/fs/proc_namespace.c | head -10
99:extern bool susfs_hide_sus_mnts_for_non_su_procs;
111:	if (susfs_hide_sus_mnts_for_non_su_procs &&
112:			r->mnt_id >= DEFAULT_KSU_MNT_ID &&
156:	if (susfs_hide_sus_mnts_for_non_su_procs &&
157:			r->mnt_id >= DEFAULT_KSU_MNT_ID &&
228:	if (susfs_hide_sus_mnts_for_non_su_procs &&
229:			r->mnt_id >= DEFAULT_KSU_MNT_ID &&

Bizde hâlâ r->mnt_id >= DEFAULT_KSU_MNT_ID var, 754a62f bunu r->mnt_id == DEFAULT_KSU_MNT_ID olarak değiştiriyor. Bu 5.4'ü etkileyen gerçek bir fix. Not edelim.
Eksik: fs/proc_namespace.c'de 3 yerde r->mnt_id >= DEFAULT_KSU_MNT_ID → r->mnt_id == DEFAULT_KSU_MNT_ID

# d974cc9 commit'inin tüm etkileri:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/d974cc9a09e22d9f475c5eea28f84e8e51f855f1

kernel_patches/include/linux/susfs_def.h:

DEFAULT_UNSHARE_KSU_MNT_ID yorum değişti
DEFAULT_KSU_MNT_ID yorum değişti
susfs_is_current_proc_umounted_app() eklendi

kernel_patches/fs/susfs.c:

susfs_add_sus_path_loop: boş pathname kontrolü eklendi, list_add_tail_rcu → list_add_tail
susfs_run_sus_path_loop(uid_t uid) → susfs_run_sus_path_loop(void), rcu_read_lock/unlock → normal lock, list_for_each_entry_rcu → list_for_each_entry, boş pathname kontrolü eklendi, uid log kaldırıldı
susfs_is_inode_sus_path tüm versiyonlarda current_uid().val < 10000 || !susfs_is_current_proc_umounted() → !susfs_is_current_proc_umounted_app()

kernel_patches/include/linux/susfs.h:

#endif → #endif // #ifdef CONFIG_KSU_SUSFS_SUS_MOUNT

kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch:

Kconfig default'ları n → y (SUS_PATH, SUS_KSTAT, OPEN_REDIRECT)
kernel_umount.c: susfs_run_sus_path_loop() çağrısı eklendi (uid parametresiz)
setuid_hook.c: susfs_run_sus_path_loop(new_uid) → susfs_run_sus_path_loop(), susfs_reorder_mnt_id() çağrısı korundu

Bizim kernel'daki durum — kontrol etmemiz gerekenler:
bashgrep -n "susfs_run_sus_path_loop\|susfs_is_current_proc_umounted_app\|list_add_tail_rcu\|list_for_each_entry_rcu" ~/aesir_kernel_oneplus_sm8350/fs/susfs.c | head -10
grep -n "susfs_run_sus_path_loop\|susfs_reorder_mnt_id" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/kernel_umount.c ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/setuid_hook.c 2>/dev/null | head -15
grep -n "susfs_is_current_proc_umounted_app" ~/aesir_kernel_oneplus_sm8350/include/linux/susfs_def.h

blackshark@blackshark:~/susfs4ksu$ grep -n "susfs_run_sus_path_loop\|susfs_is_current_proc_umounted_app\|list_add_tail_rcu\|list_for_each_entry_rcu" ~/aesir_kernel_oneplus_sm8350/fs/susfs.c | head -10
162:void susfs_run_sus_path_loop(void) {
206:	if (!susfs_is_current_proc_umounted_app()) {
blackshark@blackshark:~/susfs4ksu$ grep -n "susfs_run_sus_path_loop\|susfs_reorder_mnt_id" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/kernel_umount.c ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/setuid_hook.c 2>/dev/null | head -15
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/setuid_hook.c:57:extern void susfs_run_sus_path_loop(uid_t uid);
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/setuid_hook.c:60:extern void susfs_reorder_mnt_id(void);
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/setuid_hook.c:153:    susfs_reorder_mnt_id();
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/setuid_hook.c:157:    susfs_run_sus_path_loop(new_uid);
blackshark@blackshark:~/susfs4ksu$ grep -n "susfs_is_current_proc_umounted_app" ~/aesir_kernel_oneplus_sm8350/include/linux/susfs_def.h
83:static inline bool susfs_is_current_proc_umounted_app(void) {

Birkaç eksiklik var:

setuid_hook.c: extern void susfs_run_sus_path_loop(uid_t uid) → extern void susfs_run_sus_path_loop(void) ve susfs_run_sus_path_loop(new_uid) → susfs_run_sus_path_loop()
kernel_umount.c: susfs_run_sus_path_loop() çağrısı eksik
susfs_def.h: susfs_is_current_proc_umounted_app var ✅

kernel_umount.c'ye de bakalım:
bashgrep -n "susfs\|run_sus_path" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/kernel_umount.c | head -10

Boş döndü

kernel_umount.c'de hiç susfs yok. Not edelim.
Eksikler — d974cc9'dan:

setuid_hook.c: extern void susfs_run_sus_path_loop(uid_t uid) → extern void susfs_run_sus_path_loop(void) ve susfs_run_sus_path_loop(new_uid) → susfs_run_sus_path_loop()
kernel_umount.c: susfs_run_sus_path_loop() extern declaration + çağrısı eksik

# 723f11ee commit'inin tüm etkileri:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/723f11ee00058aa25c4e8421925cf2fbfea47344

kernel_patches/fs/susfs.c:

susfs_add_sus_path: !fi → !fi || !fi->inode.i_mapping null check
susfs_run_sus_path_loop: !fi → !fi || !fi->inode.i_mapping null check, log/path_put sırası değişti
susfs_is_inode_sus_path: üç ayrı fonksiyon → tek fonksiyon #if LINUX_VERSION_CODE ile, !inode->i_mapping null check eklendi, !fi → !fi || !fi->inode.i_mapping

kernel_patches/50_add_susfs_in_gki-android16-6.12.patch: 6.12'ye özgü değişiklikler (mnt_idmap, vfs_fstat, vfs_statx, generic_fillattr) + susfs_is_current_proc_umounted() → susfs_is_current_proc_umounted_app() geçişi birçok dosyada.
Bizim kernel'daki durum — kontrol edelim:
bashgrep -n "fi->inode.i_mapping\|!fi || \|susfs_is_current_proc_umounted_app\|susfs_is_current_proc_umounted()" ~/aesir_kernel_oneplus_sm8350/fs/susfs.c | head -15

susfs.c bu değişiklikleri içeriyor ✅. Şimdi diğer dosyaları kontrol edelim:
bashgrep -n "susfs_is_current_proc_umounted_app\|susfs_is_current_proc_umounted()" ~/aesir_kernel_oneplus_sm8350/fs/proc/task_mmu.c ~/aesir_kernel_oneplus_sm8350/fs/proc/base.c ~/aesir_kernel_oneplus_sm8350/fs/stat.c ~/aesir_kernel_oneplus_sm8350/fs/notify/fdinfo.c ~/aesir_kernel_oneplus_sm8350/fs/proc/fd.c 2>/dev/null | head -20

blackshark@blackshark:~/susfs4ksu$ grep -n "susfs_is_current_proc_umounted_app\|susfs_is_current_proc_umounted()" ~/aesir_kernel_oneplus_sm8350/fs/proc/task_mmu.c ~/aesir_kernel_oneplus_sm8350/fs/proc/base.c ~/aesir_kernel_oneplus_sm8350/fs/stat.c ~/aesir_kernel_oneplus_sm8350/fs/notify/fdinfo.c ~/aesir_kernel_oneplus_sm8350/fs/proc/fd.c 2>/dev/null | head -20
/home/blackshark/aesir_kernel_oneplus_sm8350/fs/proc/task_mmu.c:379:		susfs_is_current_proc_umounted_app()))
/home/blackshark/aesir_kernel_oneplus_sm8350/fs/proc/task_mmu.c:401:		susfs_is_current_proc_umounted_app()))
/home/blackshark/aesir_kernel_oneplus_sm8350/fs/proc/task_mmu.c:954:		susfs_is_current_proc_umounted_app()))
/home/blackshark/aesir_kernel_oneplus_sm8350/fs/proc/task_mmu.c:979:		susfs_is_current_proc_umounted_app()))
/home/blackshark/aesir_kernel_oneplus_sm8350/fs/proc/base.c:920:		susfs_is_current_proc_umounted_app()))
/home/blackshark/aesir_kernel_oneplus_sm8350/fs/proc/base.c:2397:		susfs_is_current_proc_umounted_app()))
/home/blackshark/aesir_kernel_oneplus_sm8350/fs/stat.c:59:		likely(susfs_is_current_proc_umounted_app()))
/home/blackshark/aesir_kernel_oneplus_sm8350/fs/stat.c:402:	if (likely(!susfs_is_current_proc_umounted()) && ksu_su_compat_enabled) {
/home/blackshark/aesir_kernel_oneplus_sm8350/fs/notify/fdinfo.c:94:		likely(susfs_is_current_proc_umounted_app()))
/home/blackshark/aesir_kernel_oneplus_sm8350/fs/proc/fd.c:65:			likely(susfs_is_current_proc_umounted_app()))

Tüm dosyalarda susfs_is_current_proc_umounted_app() kullanılıyor ✅. fs/stat.c satır 402'deki susfs_is_current_proc_umounted() ise exec.c hook'una ait — farklı bir kullanım, sorun değil.

# 1e6c554 commit'inin tüm etkileri:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/1e6c554909b41c8265c57811fe09682aec0b875e

Etkilenen dosya: kernel_patches/include/linux/susfs_def.h
Değişiklik: DEFAULT_UNSHARE_KSU_MNT_ID 600000 → 400000
Bizim kernel'daki durum:
bashgrep -n "DEFAULT_UNSHARE_KSU_MNT_ID" ~/aesir_kernel_oneplus_sm8350/include/linux/susfs_def.h

blackshark@blackshark:~/susfs4ksu$ grep -n "DEFAULT_UNSHARE_KSU_MNT_ID" ~/aesir_kernel_oneplus_sm8350/include/linux/susfs_def.h
46:#define DEFAULT_UNSHARE_KSU_MNT_ID 400000 /* used for mounts unshared by ksu process */

✅ Bizde zaten 400000.

# 82c7e9c commit'inin tüm etkileri:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/82c7e9c721a7b1d71381b02dfbd6b2a6b234d156

Etkilenen dosya: kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch (sadece bu)
Değişiklikler — KernelSU kaynak kodundaki değişiklikler:

allowlist.c: syscall_hook_manager.h include ve ksu_mark_running_process() çağrısı #ifndef CONFIG_KSU_SUSFS ile sarıldı
app_profile.c: bazı bloklar #ifndef CONFIG_KSU_SUSFS ile sarıldı, profile->namespaces → profile.namespaces
supercalls.c: ksu_mark_running_process() çağrısı #ifndef CONFIG_KSU_SUSFS ile sarıldı, satır numaraları kaydı

Bizim kernel'daki durum:
bashgrep -n "CONFIG_KSU_SUSFS\|ksu_mark_running_process\|syscall_hook_manager" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/allowlist.c ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/app_profile.c ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c 2>/dev/null | grep -v "^Binary" | head -20

blackshark@blackshark:~/susfs4ksu$ grep -n "CONFIG_KSU_SUSFS\|ksu_mark_running_process\|syscall_hook_manager" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/allowlist.c ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/app_profile.c ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c 2>/dev/null | grep -v "^Binary" | head -20
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/app_profile.c:28:#include "syscall_hook_manager.h"
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:15:#ifdef CONFIG_KSU_SUSFS
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:19:#endif // #ifdef CONFIG_KSU_SUSFS
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:33:#include "syscall_hook_manager.h"
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:35:#ifdef CONFIG_KSU_SUSFS
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:37:#endif // #ifdef CONFIG_KSU_SUSFS
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:131:#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:133:#endif // #ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:369:        ksu_mark_running_process();
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:449:#ifndef CONFIG_KSU_SUSFS
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:467:#endif // #ifndef CONFIG_KSU_SUSFS
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:470:#ifndef CONFIG_KSU_SUSFS
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:485:#endif // #ifndef CONFIG_KSU_SUSFS
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:489:#ifndef CONFIG_KSU_SUSFS
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:504:#endif // #ifndef CONFIG_KSU_SUSFS
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:508:#ifndef CONFIG_KSU_SUSFS
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:509:ksu_mark_running_process();
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:513:#endif // #ifndef CONFIG_KSU_SUSFS
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:858:#ifdef CONFIG_KSU_SUSFS
/home/blackshark/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c:861:#ifdef CONFIG_KSU_SUSFS_SUS_PATH

Birkaç eksiklik var:

allowlist.c: syscall_hook_manager.h #ifndef CONFIG_KSU_SUSFS ile sarılmamış
app_profile.c: syscall_hook_manager.h include hâlâ var, #ifndef CONFIG_KSU_SUSFS sarmalı yok
supercalls.c satır 369: ksu_mark_running_process() #ifndef CONFIG_KSU_SUSFS ile sarılmamış

Ayrıca supercalls.c satır 33'te #include "syscall_hook_manager.h" var ve bu #ifndef CONFIG_KSU_SUSFS içinde değil — ama bu b917aaca'dan önceki bir commit'te eklenmişti.
allowlist.c'ye de bakalım:
bashgrep -n "syscall_hook_manager\|ksu_mark_running_process" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/allowlist.c | head -10blackshark@blackshark:~/susfs4ksu$ grep -n "syscall_hook_manager\|ksu_mark_running_process" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/allowlist.c | head -1023:15Boş — allowlist.c'de syscall_hook_manager yok, bu commit zaten uygulanmış demek. ✅
app_profile.c'ye bakalım:
bashgrep -n "syscall_hook_manager\|ksu_mark_running_process\|CONFIG_KSU_SUSFS" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/app_profile.c | head -10blackshark@blackshark:~/susfs4ksu$ grep -n "syscall_hook_manager\|ksu_mark_running_process\|CONFIG_KSU_SUSFS" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/app_profile.c | head -10
28:#include "syscall_hook_manager.h"23:16app_profile.c'de syscall_hook_manager.h #ifndef CONFIG_KSU_SUSFS ile sarılmamış — eksik. Not edelim.
supercalls.c satır 369'daki ksu_mark_running_process() durumunu görelim:
bashsed -n '365,375p' ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.cblackshark@blackshark:~/susfs4ksu$ sed -n '365,375p' ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c
    ret = ksu_set_app_profile(&cmd.profile);
    if (!ret) {
        ksu_persistent_allow_list();
#ifdef KSU_KPROBES_HOOK
        ksu_mark_running_process();
#endif
    }
    return ret;
}
static int do_get_feature(void __user *arg)23:16ksu_mark_running_process() zaten #ifdef KSU_KPROBES_HOOK ile sarılmış — bizim kernelde kprobes hook kullanıyoruz mu?
bashgrep -n "KSU_KPROBES_HOOK\|CONFIG_KSU_SUSFS" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c | head -5
grep "KSU_KPROBES_HOOK" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/Kconfig | head -3blackshark@blackshark:~/susfs4ksu$ grep -n "KSU_KPROBES_HOOK\|CONFIG_KSU_SUSFS" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c | head -5
15:#ifdef CONFIG_KSU_SUSFS
19:#endif // #ifdef CONFIG_KSU_SUSFS
35:#ifdef CONFIG_KSU_SUSFS
37:#endif // #ifdef CONFIG_KSU_SUSFS
131:#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
blackshark@blackshark:~/susfs4ksu$ grep "KSU_KPROBES_HOOK" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/Kconfig | head -3
config KSU_KPROBES_HOOKBöyle gözüküyor ama kesinlikle kullanmıyoruz. Inline hooks kullanıyoruz23:17O zaman KSU_KPROBES_HOOK false olduğu için ksu_mark_running_process() zaten çalışmıyor — #ifndef CONFIG_KSU_SUSFS ile sarmak bu durumda fark yaratmıyor. ✅
Eksik — 82c7e9c'den:

app_profile.c: #include "syscall_hook_manager.h" → #ifndef CONFIG_KSU_SUSFS ile sarılmalı

# 26e8a81'den çok sayıda eksiklik var. Bunları not edelim:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/26e8a81528310dd9d95bbdb55db7eb2496788a0e

fs/susfs.c eksikleri:

seqlock yok — susfs_spin_lock_set_uname → susfs_uname_seqlock, susfs_spin_lock_set_cmdline_or_bootconfig → susfs_fake_cmdline_or_bootconfig_seqlock
susfs_spin_lock_sus_mount → WRITE_ONCE
susfs_spin_lock_enable_log → WRITE_ONCE
susfs_spin_lock_set_avc_log_spoofing → WRITE_ONCE
READ_ONCE(susfs_is_log_enabled) → SUSFS_LOGI/LOGE macrolarda
susfs_my_uname_init() kaldırıldı, my_uname = {0} ile initialize
is_susfs_uname_set flag eklendi
susfs_is_sdcard_android_data_decrypted = true → WRITE_ONCE
susfs_is_fake_cmdline_or_bootconfig_set eklendi
boş cmdline kontrolü eklendi

fs/statfs.c: susfs_is_current_proc_umounted_app() → susfs_is_current_proc_umounted(), mnt_parent loop güvenli hale getirildi, out_path_put/out_kfree label'ları eksik
fs/proc/fd.c: aynı şekilde susfs_is_current_proc_umounted_app() → susfs_is_current_proc_umounted(), mnt_parent loop güvenli, out_path_put/out_kfree eksik
fs/notify/fdinfo.c: aynı
fs/proc_namespace.c: susfs_hide_sus_mnts_for_non_su_procs → READ_ONCE(susfs_hide_sus_mnts_for_non_su_procs)
security/selinux/avc.c: susfs_is_avc_log_spoofing_enabled → READ_ONCE(susfs_is_avc_log_spoofing_enabled)
include/linux/susfs_def.h: DEFAULT_UNSHARE_KSU_MNT_ID 400000 → VFSMOUNT_MNT_FLAGS_KSU_UNSHARED_MNT 0x80000000
Ama bekle — susfs_def.h'deki bu değişiklik kritik. DEFAULT_UNSHARE_KSU_MNT_ID kaldırılıp yerine VFSMOUNT_MNT_FLAGS_KSU_UNSHARED_MNT geliyor:
bashgrep -n "DEFAULT_UNSHARE_KSU_MNT_ID\|VFSMOUNT_MNT_FLAGS_KSU_UNSHARED_MNT" ~/aesir_kernel_oneplus_sm8350/include/linux/susfs_def.hblackshark@blackshark:~/susfs4ksu$ grep -n "DEFAULT_UNSHARE_KSU_MNT_ID\|VFSMOUNT_MNT_FLAGS_KSU_UNSHARED_MNT" ~/aesir_kernel_oneplus_sm8350/include/linux/susfs_def.h
46:#define DEFAULT_UNSHARE_KSU_MNT_ID 400000 /* used for mounts unshared by ksu process */
49:#define VFSMOUNT_MNT_FLAGS_KSU_UNSHARED_MNT 0x80000000 /* set on mnt->mnt.mnt_flags for unshared ksu mounts */23:19Bizde ikisi de var — ama 26e8a81'e göre DEFAULT_UNSHARE_KSU_MNT_ID kaldırılıp sadece VFSMOUNT_MNT_FLAGS_KSU_UNSHARED_MNT kalmalı. Ama bizim namespace.c portumuzda DEFAULT_UNSHARE_KSU_MNT_ID artık kullanılmıyor — susfs_alloc_unshare_ksu_vfsmnt artık old_mnt_id direkt kullanıyor. Yani DEFAULT_UNSHARE_KSU_MNT_ID bizde kullanılıyor mu?
bashgrep -rn "DEFAULT_UNSHARE_KSU_MNT_ID" ~/aesir_kernel_oneplus_sm8350/ --include="*.c" --include="*.h" | grep -v "susfs_def.h"Boş döndü23:20DEFAULT_UNSHARE_KSU_MNT_ID hiçbir yerde kullanılmıyor — kaldırabiliriz. Ama önce son commit'leri bitirelim, sonra toplu düzeltme yaparız.

# 67cafde8 commit'ini analiz ettim. Şimdi durumumuzu kontrol edelim:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/67cafde82a0c2c4d7d57578c8db27f1f9d49795f

bashgrep -n "susfs_get_non_sus_mnt_from_mnt\|susfs_get_non_sus_vfsmnt_from_vfsmnt" ~/aesir_kernel_oneplus_sm8350/fs/namespace.c ~/aesir_kernel_oneplus_sm8350/fs/statfs.c ~/aesir_kernel_oneplus_sm8350/fs/proc/fd.c 2>/dev/null | head -20Boş döndü23:23Yani 67cafde8'deki tüm değişiklikler bizde eksik:
fs/namespace.c: susfs_get_non_sus_mnt_from_mnt() ve susfs_get_non_sus_vfsmnt_from_vfsmnt() fonksiyonları eksik
fs/proc/fd.c: susfs_get_non_sus_mnt_from_mnt forward declaration ve kullanımı eksik (mnt->mnt_id → susfs_get_non_sus_mnt_from_mnt(mnt)->mnt_id)
fs/statfs.c: Komple yeniden yazılmış vfs_statfs() — susfs_get_non_sus_vfsmnt_from_vfsmnt kullanan yeni implementasyon eksik

# 1ed6dcf — bu commit BUG 1 olarak zaten düzelttiğimiz reboot fix. Ama patch dosyasındaki satır değişikliğine bakalım:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/1ed6dcf13da25feb7aaa60ba7eb31f1a25738a20

return 0; → return -EINVAL; — susfs magic eşleşmediğinde.
Bizim düzeltmemizi kontrol edelim:
bashgrep -n "return -EINVAL\|return 0\|SUSFS_MAGIC\|KSU_INSTALL_MAGIC" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c | head -20blackshark@blackshark:~$ grep -n "return -EINVAL\|return 0\|SUSFS_MAGIC\|KSU_INSTALL_MAGIC" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c | head -20
77:    return 0;
104:    return 0;
146:    return 0;
175:    return 0;
299:    return 0;
317:    return 0;
331:    return 0;
352:    return 0;
400:    return 0;
420:    return 0;
425:        return -EINVAL;
518:        return -EINVAL;
526:    return 0;
549:    return 0;
567:    return 0;
580:        return -EINVAL;
624:            return 0;
670:            return 0;
692:            return 0;
714:            return 0;Show more23:24Daha fazla görmem gerekiyor — ksu_handle_sys_reboot fonksiyonunu bulalım:
bashgrep -n "ksu_handle_sys_reboot\|SUSFS_MAGIC\|KSU_INSTALL_MAGIC" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c | head -20blackshark@blackshark:~$ grep -n "ksu_handle_sys_reboot\|SUSFS_MAGIC\|KSU_INSTALL_MAGIC" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c | head -20
847:int ksu_handle_sys_reboot(int magic1, int magic2, unsigned int cmd,
850:    if (magic1 != KSU_INSTALL_MAGIC1)
860:    if (magic2 == SUSFS_MAGIC && current_uid().val == 0) {
954:    if (magic2 == KSU_INSTALL_MAGIC2) {
1122:    return ksu_handle_sys_reboot(magic1, magic2, cmd, (void __user **)&arg4);23:24bashsed -n '847,960p' ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.cblackshark@blackshark:~$ sed -n '847,960p' ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c
int ksu_handle_sys_reboot(int magic1, int magic2, unsigned int cmd,
              void __user **arg)
{
    if (magic1 != KSU_INSTALL_MAGIC1)
        return -EINVAL;
#ifdef CONFIG_KSU_DEBUG
    pr_info("sys_reboot: intercepted call! magic: 0x%x id: %d\n", magic1,
        magic2);
#endif
#ifdef CONFIG_KSU_SUSFS
    // If magic2 is susfs and current process is root
    if (magic2 == SUSFS_MAGIC && current_uid().val == 0) {
#ifdef CONFIG_KSU_SUSFS_SUS_PATH
        if (cmd == CMD_SUSFS_ADD_SUS_PATH) {
            susfs_add_sus_path(arg);
            return 0;
        }
        if (cmd == CMD_SUSFS_ADD_SUS_PATH_LOOP) {
            susfs_add_sus_path_loop(arg);
            return 0;
        }
        if (cmd == CMD_SUSFS_SET_ANDROID_DATA_ROOT_PATH) {
            susfs_set_i_state_on_external_dir(arg);
            return 0;
        }
        if (cmd == CMD_SUSFS_SET_SDCARD_ROOT_PATH) {
            susfs_set_i_state_on_external_dir(arg);
            return 0;
        }
#endif //#ifdef CONFIG_KSU_SUSFS_SUS_PATH
#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
        if (cmd == CMD_SUSFS_HIDE_SUS_MNTS_FOR_NON_SU_PROCS) {
            susfs_set_hide_sus_mnts_for_non_su_procs(arg);
            return 0;
        }
#endif //#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
#ifdef CONFIG_KSU_SUSFS_SUS_KSTAT
        if (cmd == CMD_SUSFS_ADD_SUS_KSTAT) {
            susfs_add_sus_kstat(arg);
            return 0;
        }
        if (cmd == CMD_SUSFS_UPDATE_SUS_KSTAT) {
            susfs_update_sus_kstat(arg);
            return 0;
        }
        if (cmd == CMD_SUSFS_ADD_SUS_KSTAT_STATICALLY) {
            susfs_add_sus_kstat(arg);
            return 0;
        }
#endif //#ifdef CONFIG_KSU_SUSFS_SUS_KSTAT
#ifdef CONFIG_KSU_SUSFS_TRY_UMOUNT
        if (cmd == CMD_SUSFS_ADD_TRY_UMOUNT) {
            return add_try_umount(arg);
        }
#endif //#ifdef CONFIG_KSU_SUSFS_TRY_UMOUNT
#ifdef CONFIG_KSU_SUSFS_SPOOF_UNAME
        if (cmd == CMD_SUSFS_SET_UNAME) {
            susfs_set_uname(arg);
            return 0;
        }
#endif //#ifdef CONFIG_KSU_SUSFS_SPOOF_UNAME
#ifdef CONFIG_KSU_SUSFS_ENABLE_LOG
        if (cmd == CMD_SUSFS_ENABLE_LOG) {
            susfs_enable_log(arg);
            return 0;
        }
#endif //#ifdef CONFIG_KSU_SUSFS_ENABLE_LOG
#ifdef CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG
        if (cmd == CMD_SUSFS_SET_CMDLINE_OR_BOOTCONFIG) {
            susfs_set_cmdline_or_bootconfig(arg);
            return 0;
        }
#endif //#ifdef CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG
#ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
        if (cmd == CMD_SUSFS_ADD_OPEN_REDIRECT) {
            susfs_add_open_redirect(arg);
            return 0;
        }
#endif //#ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
#ifdef CONFIG_KSU_SUSFS_SUS_MAP
        if (cmd == CMD_SUSFS_ADD_SUS_MAP) {
            susfs_add_sus_map(arg);
            return 0;
        }
#endif // #ifdef CONFIG_KSU_SUSFS_SUS_MAP
        if (cmd == CMD_SUSFS_ENABLE_AVC_LOG_SPOOFING) {
            susfs_set_avc_log_spoofing(arg);
            return 0;
        }
        if (cmd == CMD_SUSFS_SHOW_ENABLED_FEATURES) {
            susfs_get_enabled_features(arg);
            return 0;
        }
        if (cmd == CMD_SUSFS_SHOW_VARIANT) {
            susfs_show_variant(arg);
            return 0;
        }
        if (cmd == CMD_SUSFS_SHOW_VERSION) {
            susfs_show_version(arg);
            return 0;
        }
        return 0;
    }
#endif // #ifdef CONFIG_KSU_SUSFS
    // Check if this is a request to install KSU fd
    if (magic2 == KSU_INSTALL_MAGIC2) {
        int fd = ksu_install_fd();
        // downstream: dereference all arg usage!
        if (copy_to_user((void __user *)*arg, &fd, sizeof(fd))) {
            pr_err("install ksu fd reply err\n");
#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 11, 0)
        close_fd(fd);Show more23:25Satır 957'deki return 0; → return -EINVAL; olmalı. Bu 1ed6dcf'nin düzeltmesi:
bashsed -n '955,962p' ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.cblackshark@blackshark:~$ sed -n '955,962p' ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c
        int fd = ksu_install_fd();
        // downstream: dereference all arg usage!
        if (copy_to_user((void __user *)*arg, &fd, sizeof(fd))) {
            pr_err("install ksu fd reply err\n");
#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 11, 0)
        close_fd(fd);
#else
        __close_fd(current->files, fd);23:25Daha yukarı bakmam lazım — return 0; olan satırı bulmam gerekiyor:
bashsed -n '948,956p' ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.cblackshark@blackshark:~$ sed -n '948,956p' ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/supercalls.c
            return 0;
        }
        return 0;
    }
#endif // #ifdef CONFIG_KSU_SUSFS
    // Check if this is a request to install KSU fd
    if (magic2 == KSU_INSTALL_MAGIC2) {
        int fd = ksu_install_fd();
        // downstream: dereference all arg usage!23:25Satır 950: return 0; — bu magic2 == SUSFS_MAGIC bloğunun sonundaki fallthrough return. 1ed6dcf'ye göre bu return -EINVAL; olmalı.
Eksik — 1ed6dcf'den:

supercalls.c satır 950: return 0; → return -EINVAL;

# e9629adb — fs/namei.c değişiklikleri. Bizim durumumuzu kontrol edelim:

https://gitlab.com/simonpunk/susfs4ksu/-/commit/e9629adbf274c3270bdfcf7970c129256a77f248

bashgrep -n "susfs_is_inode_sus_path\|susfs_fake_qstr_name\|found_sus_path\|lookup_dcache\|lookup_one_qstr_excl\|__lookup_slow" ~/aesir_kernel_oneplus_sm8350/fs/namei.c | head -20blackshark@blackshark:~$ grep -n "susfs_is_inode_sus_path\|susfs_fake_qstr_name\|found_sus_path\|lookup_dcache\|lookup_one_qstr_excl\|__lookup_slow" ~/aesir_kernel_oneplus_sm8350/fs/namei.c | head -20
48:extern bool susfs_is_inode_sus_path(struct inode *inode);
49:extern const struct qstr susfs_fake_qstr_name;
1590:static struct dentry *lookup_dcache(const struct qstr *name,
1617:    struct dentry *dentry = lookup_dcache(name, base, flags), *old;
1662:            susfs_is_inode_sus_path(dentry->d_inode))
1718:            susfs_is_inode_sus_path(dentry->d_inode))
1750:static struct dentry *__lookup_slow(const struct qstr *name,
1758:    bool found_sus_path = false;
1780:                    if (found_sus_path) {
1781:                        dentry = d_alloc_parallel(dir, &susfs_fake_qstr_name, &wq);
1800:    if (is_nd_flags_lookup_last && !found_sus_path && dentry && !IS_ERR(dentry) && dentry->d_inode &&
1801:        susfs_is_inode_sus_path(dentry->d_inode))
1806:        dentry = d_alloc_parallel(dir, &susfs_fake_qstr_name, &wq);
1807:        found_sus_path = true;
1821:    res = __lookup_slow(name, dir, flags);
2224:        if (dentry->d_inode && susfs_is_inode_sus_path(dentry->d_inode)) {
2659:    return lookup_dcache(&this, base, 0);
2686:    dentry = lookup_dcache(&this, base, 0);
2687:    return dentry ? dentry : __lookup_slow(&this, base, 0);
2714:    ret = lookup_dcache(&this, base, 0);Show more23:27Bizde zaten susfs path hook'ları var ama e9629adb'nin yeni eklemeleri farklı. Kontrol edelim:
bashsed -n '1590,1640p' ~/aesir_kernel_oneplus_sm8350/fs/namei.cblackshark@blackshark:~$ sed -n '1590,1640p' ~/aesir_kernel_oneplus_sm8350/fs/namei.c
static struct dentry *lookup_dcache(const struct qstr *name,
                    struct dentry *dir,
                    unsigned int flags)
{
    struct dentry *dentry = d_lookup(dir, name);
    if (dentry) {
        int error = d_revalidate(dentry, flags);
        if (unlikely(error <= 0)) {
            if (!error)
                d_invalidate(dentry);
            dput(dentry);
            return ERR_PTR(error);
        }
    }
    return dentry;
}
/*
 * Parent directory has inode locked exclusive.  This is one
 * and only case when ->lookup() gets called on non in-lookup
 * dentries - as the matter of fact, this only gets called
 * when directory is guaranteed to have no in-lookup children
 * at all.
 */
static struct dentry *__lookup_hash(const struct qstr *name,
        struct dentry *base, unsigned int flags)
{
    struct dentry *dentry = lookup_dcache(name, base, flags), *old;
    struct inode *dir = base->d_inode;
    if (dentry)
        return dentry;
    /* Don't create child dentry for a dead directory. */
    if (unlikely(IS_DEADDIR(dir)))
        return ERR_PTR(-ENOENT);
    dentry = d_alloc(base, name);
    if (unlikely(!dentry))
        return ERR_PTR(-ENOMEM);
    old = dir->i_op->lookup(dir, dentry, flags);
    if (unlikely(old)) {
        dput(dentry);
        dentry = old;
    }
    return dentry;
}
static int lookup_fast(struct nameidata *nd,
               struct path *path, struct inode **inode,Show more23:27lookup_dcache'de e9629adb'nin eklediği susfs_is_inode_sus_path hook'u bizde yok. Ayrıca lookup_one_qstr_excl yerine bizde __lookup_hash var — bu 5.4 kernel farkı.
e9629adb'nin 5.4'e uygulanabilir kısımlarını bulmak için lookup_fast ve __lookup_slow fonksiyonlarına bakalım:
bashsed -n '1640,1760p' ~/aesir_kernel_oneplus_sm8350/fs/namei.cblackshark@blackshark:~$ sed -n '1640,1760p' ~/aesir_kernel_oneplus_sm8350/fs/namei.c
               struct path *path, struct inode **inode,
               unsigned *seqp)
{
    struct vfsmount *mnt = nd->path.mnt;
    struct dentry *dentry, *parent = nd->path.dentry;
    int status = 1;
    int err;
#ifdef CONFIG_KSU_SUSFS_SUS_PATH
    bool is_nd_flags_lookup_last_and_open_last = (nd->flags & (ND_FLAGS_LOOKUP_LAST | ND_FLAGS_OPEN_LAST));
#endif
    /*
     * Rename seqlock is not required here because in the off chance
     * of a false negative due to a concurrent rename, the caller is
     * going to fall back to non-racy lookup.
     */
    if (nd->flags & LOOKUP_RCU) {
        unsigned seq;
        bool negative;
        dentry = __d_lookup_rcu(parent, &nd->last, &seq);
#ifdef CONFIG_KSU_SUSFS_SUS_PATH
        if (is_nd_flags_lookup_last_and_open_last && dentry && !IS_ERR(dentry) && dentry->d_inode &&
            susfs_is_inode_sus_path(dentry->d_inode))
        {
            if (d_in_lookup(dentry))
                d_lookup_done(dentry);
            // no dput() here, __d_lookup_rcu() does not take the dentry->d_lockref.count
            dentry = NULL;
        }
#endif
        if (unlikely(!dentry)) {
            if (unlazy_walk(nd))
                return -ECHILD;
            return 0;
        }
        /*
         * This sequence count validates that the inode matches
         * the dentry name information from lookup.
         */
        *inode = d_backing_inode(dentry);
        negative = d_is_negative(dentry);
        if (unlikely(read_seqcount_retry(&dentry->d_seq, seq)))
            return -ECHILD;
        /*
         * This sequence count validates that the parent had no
         * changes while we did the lookup of the dentry above.
         *
         * The memory barrier in read_seqcount_begin of child is
         *  enough, we can use __read_seqcount_retry here.
         */
        if (unlikely(__read_seqcount_retry(&parent->d_seq, nd->seq)))
            return -ECHILD;
        *seqp = seq;
        status = d_revalidate(dentry, nd->flags);
        if (likely(status > 0)) {
            /*
             * Note: do negative dentry check after revalidation in
             * case that drops it.
             */
            if (unlikely(negative))
                return -ENOENT;
            path->mnt = mnt;
            path->dentry = dentry;
            if (likely(__follow_mount_rcu(nd, path, inode, seqp)))
                return 1;
        }
        if (unlazy_child(nd, dentry, seq))
            return -ECHILD;
        if (unlikely(status == -ECHILD))
            /* we'd been told to redo it in non-rcu mode */
            status = d_revalidate(dentry, nd->flags);
    } else {
        dentry = __d_lookup(parent, &nd->last);
#ifdef CONFIG_KSU_SUSFS_SUS_PATH
        if (is_nd_flags_lookup_last_and_open_last && dentry && !IS_ERR(dentry) && dentry->d_inode &&
            susfs_is_inode_sus_path(dentry->d_inode))
        {
            if (d_in_lookup(dentry))
                d_lookup_done(dentry);
            dput(dentry);
            dentry = NULL;
        }
#endif
        if (unlikely(!dentry))
            return 0;
        status = d_revalidate(dentry, nd->flags);
    }
    if (unlikely(status <= 0)) {
        if (!status)
            d_invalidate(dentry);
        dput(dentry);
        return status;
    }
    if (unlikely(d_is_negative(dentry))) {
        dput(dentry);
        return -ENOENT;
    }
    path->mnt = mnt;
    path->dentry = dentry;
    err = follow_managed(path, nd);
    if (likely(err > 0))
        *inode = d_backing_inode(path->dentry);
    return err;
}
/* Fast lookup failed, do it the slow way */
static struct dentry *__lookup_slow(const struct qstr *name,
                    struct dentry *dir,
                    unsigned int flags)
{
    struct dentry *dentry, *old;
    struct inode *inode = dir->d_inode;
    DECLARE_WAIT_QUEUE_HEAD_ONSTACK(wq);
#ifdef CONFIG_KSU_SUSFS_SUS_PATH
    bool found_sus_path = false;
    bool is_nd_flags_lookup_last = (flags & ND_FLAGS_LOOKUP_LAST);
#endifShow more23:28Bizim lookup_fast ve __lookup_slow'da hook'lar zaten var. e9629adb'nin eklediği lookup_dcache hook'u bizde eksik. Kontrol edelim:
bashsed -n '1590,1606p' ~/aesir_kernel_oneplus_sm8350/fs/namei.cblackshark@blackshark:~$ sed -n '1590,1606p' ~/aesir_kernel_oneplus_sm8350/fs/namei.c
static struct dentry *lookup_dcache(const struct qstr *name,
                    struct dentry *dir,
                    unsigned int flags)
{
    struct dentry *dentry = d_lookup(dir, name);
    if (dentry) {
        int error = d_revalidate(dentry, flags);
        if (unlikely(error <= 0)) {
            if (!error)
                d_invalidate(dentry);
            dput(dentry);
            return ERR_PTR(error);
        }
    }
    return dentry;
}Show more23:28lookup_dcache'de e9629adb'nin eklediği hook yok. Eklenecek:
c#ifdef CONFIG_KSU_SUSFS_SUS_PATH
	if (dentry && !IS_ERR(dentry) && dentry->d_inode && susfs_is_inode_sus_path(dentry->d_inode)) {
		if (d_in_lookup(dentry))
			d_lookup_done(dentry);
		dput(dentry);
		return NULL;
	}
#endif
return dentry;'den hemen önce. Ayrıca filldir return değeri düzeltmesi var — kontrol edelim:
bashgrep -n "susfs_is_inode_sus_path\|return true\|return 0" ~/aesir_kernel_oneplus_sm8350/fs/namei.c | grep -A1 -B1 "susfs_is_inode_sus_path" | head -20blackshark@blackshark:~$ grep -n "susfs_is_inode_sus_path\|return true\|return 0" ~/aesir_kernel_oneplus_sm8350/fs/namei.c | grep -A1 -B1 "susfs_is_inode_sus_path" | head -20
48:extern bool susfs_is_inode_sus_path(struct inode *inode);
326:        return 0;
--
1583:    return 0;
1662:            susfs_is_inode_sus_path(dentry->d_inode))
1673:            return 0;
1718:            susfs_is_inode_sus_path(dentry->d_inode))
1727:            return 0;
1801:        susfs_is_inode_sus_path(dentry->d_inode))
1848:    return 0;
--
2208:        return 0;
2224:        if (dentry->d_inode && susfs_is_inode_sus_path(dentry->d_inode)) {
2273:                return 0;
--
3319:                return 0;
3383:        susfs_is_inode_sus_path(dentry->d_inode))
3503:    return 0;Show more23:28return 0 kullanılıyor, return true yok. e9629adb'deki filldir fix bizde zaten doğru. ✅
e9629adb'den tek eksik:

lookup_dcache: return dentry;'den önce susfs_is_inode_sus_path hook'u eksik

# b515ce75 — şimdi analiz ediyorum.

https://gitlab.com/simonpunk/susfs4ksu/-/commit/b515ce7596b4ed5861a91bd57b0254d1aa4c5e32

Bu commit kernel_umount.c ve setuid_hook.c'yi önemli ölçüde değiştiriyor. Bizim durumumuzu kontrol edelim:
bashgrep -n "susfs_run_sus_path_loop\|susfs_handle_setuid_tw\|ksu_handle_extra_susfs_work\|susfs_zygote_sid\|ksu_cred" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/setuid_hook.c | head -20
bashgrep -n "susfs_run_sus_path_loop\|up_read.*mount_list_lock" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/kernel_umount.c | head -10blackshark@blackshark:~$ grep -n "susfs_run_sus_path_loop\|susfs_handle_setuid_tw\|ksu_handle_extra_susfs_work\|susfs_zygote_sid\|ksu_cred" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/setuid_hook.c | head -20
55:extern u32 susfs_zygote_sid;
57:extern void susfs_run_sus_path_loop(uid_t uid);
80:    if (!susfs_is_sid_equal(current_cred(), susfs_zygote_sid)) {
157:    susfs_run_sus_path_loop(new_uid);
blackshark@blackshark:~$ grep -n "susfs_run_sus_path_loop\|up_read.*mount_list_lock" ~/aesir_kernel_oneplus_sm8350/drivers/kernelsu/kernel_umount.c | head -10
125:    up_read(&mount_list_lock);23:31Tablo netleşti. b515ce75 çok büyük bir yeniden yapılandırma. Bizde:
setuid_hook.c:

susfs_run_sus_path_loop(uid_t uid) — eski imza, yeni imza void
susfs_handle_setuid_tw struct + susfs_handle_setuid_tw_func() + ksu_handle_extra_susfs_work() — tümü eksik
ksu_cred extern eksik

kernel_umount.c:

susfs_run_sus_path_loop() extern + çağrısı eksik (up_read(&mount_list_lock) sonrasına)

Şimdi listemizi güncelleyelim — setuid_hook.c için önceki notumuz değişti:
drivers/kernelsu/setuid_hook.c — susfs_run_sus_path_loop(uid_t uid) → void + direct call kaldırılıp ksu_handle_extra_susfs_work() ile değiştirilmeli + tüm yeni struct/fonksiyonlar eklenmeli
drivers/kernelsu/kernel_umount.c — susfs_run_sus_path_loop() extern + up_read sonrasına çağrı eklenmeli

